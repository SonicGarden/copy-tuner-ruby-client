# This configuration was generated by
# `rubocop --auto-gen-config --exclude-limit 300`
# on 2026-07-08 22:16:51 UTC using RuboCop version 1.88.1.
# The point is for the user to remove these configuration records
# one by one as the offenses are removed from the code base.
# Note that changes in the inspected code, or installation of new
# versions of RuboCop, may require this file to be generated again.

# Offense count: 2
# 要判断: have_config_option カスタムマッチャーの .default / .overridable オプション有無で
# 検証内容を切り替えるDSL実装。無条件化すると @default / @overridable 未設定時に誤検証になる。
Sgcop/Rspec/ConditionalExample:
  Exclude:
    - 'spec/copy_tuner_client/configuration_spec.rb'

# Offense count: 2
# 要判断: copyray_middleware_spec.rb の headers は親の app let が参照する名前で、
# turbo stream / json レスポンスを検証するため context ごとに Content-Type を差し替える意図的な設計。
Sgcop/Rspec/NoLetOverride:
  Exclude:
    - 'spec/copy_tuner_client/copyray_middleware_spec.rb'

# Offense count: 3
# Configuration parameters: TargetMatchers, AllowedPatterns.
# TargetMatchers: have_content, have_text, have_css, have_selector, have_title, eq, eql, equal, include, match
# AllowedPatterns: _path$, _url$, ^I18n\.t$, ^I18n\.l$, ^id$, ^to_date$, ^to_i$, ^to_s$
# 要判断: configuration_spec.rb の3件は hash と config（複数optionにわたるループ、public?、merge）が
# 実際に一致することを検証するテストで、比較先をリテラルに書き出せない構造。
Sgcop/Rspec/NoMethodCallInExpectation:
  Exclude:
    - 'spec/copy_tuner_client/configuration_spec.rb'

